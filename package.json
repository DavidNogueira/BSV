{
  "name": "lab-l12",
  "version": "1.0.0",
  "description": "Lab L-12: Event Logger",
  "private": true,
  "workspaces": [
    "frontend",
    "authserver"
  ],
  "scripts": {
    "lars": "lars",
    "lars:config": "lars config",
    "start": "lars start",
    "build": "cd frontend && npm run build && cd ../authserver && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@bsv/lars": "^1.5.0",
    "@bsv/sdk": "^2.1.6"
  }
}
